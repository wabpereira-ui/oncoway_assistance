# Em desenvolvimento, deixe vazio para usar o proxy do Vite (vite.config.js).
# Em produção, se API e front estiverem em origens diferentes, use a URL pública da API:
# VITE_API_BASE=https://api.seudominio.com

VITE_API_BASE=
